;; :initial matters — without it the var is "" until the first poll lands, and the
;; `for` below fails to parse that as json-value on every eww start.
(defpoll workspaces :interval "1s" :initial "[]"
  "~/.config/eww/scripts/workspace.sh")

(defwidget workspace-widget []
  (box :class "workspace-box" :spacing 6
    (for ws in workspaces
      (button :class {ws.active ? "ws-dot-active" : "ws-dot"}
              :onclick "hyprctl dispatch workspace ${ws.id}"
        (label :text {ws.active ? "●" : "○"})))))
