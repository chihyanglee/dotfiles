general {
  col.active_border = rgba({{ colors.primary.default.hex_stripped }}ff)
  col.inactive_border = rgba({{ colors.outline.default.hex_stripped }}cc)
}
decoration {
  shadow {
    color = rgba({{ colors.shadow.default.hex_stripped }}88)
  }
}
